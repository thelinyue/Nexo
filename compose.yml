name: nexo

# Nexo v0.2.0 Server 部署。使用空数据目录进行全新安装。
services:
  nexo-server:
    image: ghcr.io/thelinyue/nexo-server:0.2.0
    container_name: nexo-server
    network_mode: host
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      NEXO_PUBLIC_URL: ${NEXO_PUBLIC_URL:-}
      NEXO_TRUSTED_PROXIES: ${NEXO_TRUSTED_PROXIES:-}
      NEXO_PUBLIC_IPS: ${NEXO_PUBLIC_IPS:-}
    volumes:
      - ./data/nexo:/data/nexo
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://127.0.0.1:8280/health >/dev/null"]
      interval: 10s
      timeout: 3s
      retries: 6
