# 安全策略

## 支持版本

当前仅维护 `0.2.x` 系列的最新稳定版本。安全修复会通过新的补丁版本发布，不会移动
或覆盖已经公开的 Git 版本标签和容器数字版本标签。

各组件的 `latest` 镜像标签随其最新稳定版更新；预发布版本不会更新 `latest`，历史
版本补发也不会使其倒退。Server 与 Agent 独立发布，仅更新实际发布组件的别名。
`latest` 不会自动更新运行中的容器；部署者需按发布说明更新受影响组件并保留完整备份。

## 报告漏洞

请使用 GitHub 仓库 **Security → Advisories → Report a vulnerability** 私密报告。
不要在公开 Issue 中提交漏洞细节、Token、证书私钥、数据库或部署日志中的凭据。

报告时请说明受影响版本、部署方式、复现步骤和实际影响。项目维护者确认问题后，
会通过 GitHub Security Advisory 协调修复与披露。
