# Nexo 项目约定

## 发布规范（硬约束）

### 组件独立发布

1. 只有存在实际代码变化的组件才允许发布。Server 和 Agent 独立判断；发布元数据、版本号、文档和锁文件变化不算组件代码变化。
2. Web、数据库迁移和 Headscale Adapter 属于 Server 组件。共享运行时代码影响哪一端，就把哪一端视为有代码变化。
3. 未发生代码变化的组件禁止为当前版本生成镜像、部署包或升级指引。本次版本清单明确声明实际发布组件。
4. Server 与 Agent 的镜像标签可以不同。部署时只拉取并重建发生代码变化的服务，保留另一端的原版本和节点身份。

## 注释和日志

1. 每一个关键类的设计和实现，补充完善的中文注释，好的注释可以帮助项目其他成员看懂关键设计思路，有利于后续的扩展和迭代。
2. 日志可以使用中文输出明确的错误信息，这是一款开源软件，非开发者可能也会部署，遇到任何错误，他们需要能够看懂。

## 项目约定

### 1. 编码前思考

不要假设。不要隐藏困惑。呈现权衡。

LLM 经常默默选择一种解释然后执行。这个原则强制明确推理：

- 明确说明假设 — 如果不确定，询问而不是猜测。
- 呈现多种解释 — 当存在歧义时，不要默默选择。
- 适时提出异议 — 如果存在更简单的方法，说出来。
- 困惑时停下来 — 指出不清楚的地方并要求澄清。

### 2. 简洁优先

用最少的代码解决问题。不要过度推测。

- 不要添加要求之外的功能。
- 不要为一次性代码创建抽象。
- 不要添加未要求的灵活性或可配置性。
- 不要为不可能发生的场景做错误处理。
- 如果 200 行代码可以写成 50 行，重写它。

### 3. 精准修改

只碰必须碰的。只清理自己造成的混乱。

- 不要改进相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你更倾向于不同的写法。
- 每一行修改都应该能直接追溯到用户的请求。
- 编辑产生的孤儿代码可以删除；预先存在的死代码不要顺手删除。

### 4. 目标驱动执行

把指令转化为可验证的目标：为无效输入编写重现测试，让测试通过；重构前后都执行测试；多步骤任务列出步骤和验证方式，并循环验证直到成功。

默认不新增 hash、冻结 contract、baseline 或 gate。只有能明确说出具体失败场景，并说明 Git、版本号、主键、事务、唯一约束、类型和普通测试为什么不足时，才允许加入。同时保留认证、数据安全、不可逆操作和正式发布等安全边界。
